{
  "name": "promiseandasyncawait",
  "version": "1.0.0",
  "description": "* ``cd server && npm start`` to start the server * ``node index.js`` to run the script",
  "main": "AsyncAwait.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5"
  }
}
